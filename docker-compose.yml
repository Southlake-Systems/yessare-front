name: yessare-front

services:
  web:
    build:
      context: .
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-https://api.yessaretools.com}
    restart: unless-stopped
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
